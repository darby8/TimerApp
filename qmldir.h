#ifndef Error in " 'qmldir
.h '.toUpperCase().replace(/[.]/g, '_ ')": SyntaxError: Expected token `;'
#define Error in " 'qmldir
    .h '.toUpperCase().replace(/[.]/g, '_ ')": SyntaxError: Expected token `;'

#include <QAbstractItemModel>
    Error in " Cpp.openNamespaces('qmldir
    ')": SyntaxError: Expected token `)' class qmldir : public QAbstractItemModel
{
    Q_OBJECT

public:
    explicit qmldir(QObject *parent = nullptr);

    // Header:
    QVariant headerData(int section,
                        Qt::Orientation orientation,
                        int role = Qt::DisplayRole) const override;

    // Basic functionality:
    QModelIndex index(int row, int column, const QModelIndex &parent = QModelIndex()) const override;
    QModelIndex parent(const QModelIndex &index) const override;

    int rowCount(const QModelIndex &parent = QModelIndex()) const override;
    int columnCount(const QModelIndex &parent = QModelIndex()) const override;

    QVariant data(const QModelIndex &index, int role = Qt::DisplayRole) const override;

private:
};
Error in " Cpp.closeNamespaces('qmldir
    ')": SyntaxError: Expected token `)'
#endif // Error in " 'qmldir
        .h '.toUpperCase().replace(/[.]/g, '_ ')": SyntaxError: Expected token `;'
